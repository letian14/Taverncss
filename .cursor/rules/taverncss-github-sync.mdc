---
description: Taverncss 项目 GitHub 同步规范
alwaysApply: true
---

# Taverncss — GitHub 同步规范

## 仓库信息

- **仓库地址**：https://github.com/letian14/Taverncss
- **账号**：letian14@hotmail.com
- **本地路径**：`C:\Users\letia\Desktop\css`
- **远程分支**：`main`
- **CDN 引用格式**：`https://cdn.jsdelivr.net/gh/letian14/Taverncss/{文件名}`

## Git 操作规范

1. 每次完成 CSS 组件开发后，执行 `git add` + `git commit` + `git push` 同步到 GitHub
2. commit 信息使用中文，简洁描述本次改动（如 `fix: 修复指南针 tooltip 层级遮挡问题`）
3. 推送前确认已测试（通过 `index.html` 预览）
4. 酒馆指令中使用 CDN 地址引用 CSS，不直接使用本地路径

## 工作流

```
修改 CSS 文件 → 本地 index.html 调试 → 确认效果 → git commit → git push → 更新酒馆面具指令中的 link 地址
```

## 维护清单

- [ ] 本地修改后必须先在 `index.html` 确认效果
- [ ] 推送前确认 `index.html` 中的本地引用路径正确
- [ ] 酒馆面具指令中使用 `https://cdn.jsdelivr.net/gh/letian14/Taverncss/{文件名}` 引用
- [ ] 重大改动更新版本号（文件名中的 v1.0/v1.1 等）
