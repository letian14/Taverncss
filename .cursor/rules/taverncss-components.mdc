---
description: Taverncss 项目核心规范
alwaysApply: true
---

# Taverncss CSS 组件库项目规范

## 项目背景

这是一个用于 AI 酒馆（Siliconflow/SillyTavern 等第三方平台）的 CSS 组件库。用户游玩时面具指令字数上限为 4000 字，所以复杂样式必须外置到独立 CSS 文件中，通过 `<link>` 引用。

## 核心原则

1. **样式外置优先**：能用 CSS 实现的绝不写进酒馆指令
2. **本地调试**：每次修改 CSS 后通过 `index.html` 预览确认效果
3. **GitHub 托管**：所有 CSS 文件同步到 https://github.com/letian14/Taverncss
4. **版本管理**：遵循语义化版本（v1.0, v1.1, v1.2...）
5. **简洁提示词**：酒馆指令里只放核心逻辑和 HTML 模板，样式全部外置

## 项目结构

```
Taverncss/
├── compass-default-v1.0.css     ← 指南针组件
├── inventory-default-v1.8.css   ← 背包/物资组件
├── magic-book-v1.css            ← 魔法书组件
├── index.html                   ← 本地调试页面
├── README.md
└── .cursor/rules/               ← Cursor AI 持久规则
```

## CSS 文件规范

- 文件名格式：`{组件名}-default-v{主版本}.{次版本}.css`
- 主题色通过 CSS 变量 `--accent` 暴露，允许外部传入十六进制单色调
- 组件必须使用 `.compass-wrap`、`.inventory-wrap` 等带命名空间前缀的类名，避免冲突
- 每个 CSS 变量只声明一次，在根规则 `.compass-wrap` 等顶层容器内定义

## 组件开发流程

1. 在 `index.html` 中编写 HTML 结构并嵌入样式调试
2. 确认效果后，抽取为独立 `.css` 文件
3. 上传到 GitHub（CDN 地址：`https://cdn.jsdelivr.net/gh/letian14/Taverncss/{文件名}`）
4. 在酒馆面具指令中使用 `<link>` 引用 CDN 地址
5. 本地 `index.html` 中可通过本地相对路径 `<link rel="stylesheet" href="compass-default-v1.0.css">` 调试

## 指南针组件规范（重点）

### HTML 结构

```html
<div class="compass-wrap" style="--accent:#7cc4ff;">
  <!-- 6个隐藏 radio，id 必须为 t-reset/t-nw/t-ne/t-w/t-se/t-s -->
  <input type="radio" class="d-input" name="dial" id="t-reset" checked="">
  <input type="radio" class="d-input" name="dial" id="t-nw">
  <input type="radio" class="d-input" name="dial" id="t-ne">
  <input type="radio" class="d-input" name="dial" id="t-w">
  <input type="radio" class="d-input" name="dial" id="t-se">
  <input type="radio" class="d-input" name="dial" id="t-s">
  <!-- 重置区域 -->
  <label for="t-reset" class="reset-area"></label>
  <!-- 表盘 -->
  <div class="compass-stage">
    <div class="compass-dial">
      <span class="dir dir-n">N</span>
      <span class="dir dir-s">S</span>
      <span class="dir dir-w">W</span>
      <span class="dir dir-e">E</span>
      <div class="core"></div>
      <!-- 5个地标 marker -->
      <label for="t-nw" class="marker" style="--x:25%;--y:25%;" data-side="top" data-align="left">
        <div class="tip">
          <div class="tip-head"><b>地标名称</b><span>距离 · 方位</span></div>
          <div class="tip-body">地标描述文字，根据剧情动态填写真实情报。</div>
        </div>
      </label>
      <!-- t-ne, t-w, t-se, t-s 同理 -->
    </div>
  </div>
</div>
```

### marker 定位规则

- `--x`、`--y` 范围：`15%` ~ `85%`（避免贴边溢出）
- `data-side`：`top` 用于 --y < 60%，`bottom` 用于 --y >= 60%
- `data-align`：`left` 用于 --x < 30%，`right` 用于 --x > 70%，其余用 `center`
- 5个 marker 的 `for` 属性必须分别对应 `t-nw`、`t-ne`、`t-w`、`t-se`、`t-s`，不得重复

### 交互逻辑

- 鼠标悬停 marker：tooltip 显示
- 点击 marker：表盘 3D 倾斜 + tooltip 常驻
- 点击空白区域：恢复初始状态（t-reset checked）

### tooltip 结构

- `.tip` 内必须有 `.tip-head`（标题+距离方位）和 `.tip-body`（描述文字）
- `.tip-head` 包含 `<b>` 加粗标题和 `<span>` 辅助文字
- 不要在 tooltip 内使用 `<!--AUTO_HTML_GROUP_X-->` 占位符，保持干净结构

## 组件隔离原则

- 组件之间禁止直接相邻（指南针前后必须各空一行）
- 指南针最外层：`<div class="compass-wrap" style="--accent:色值;">`
- 组件外层容器禁止额外修改 margin/padding/position/z-index/overflow
- `compass-wrap` 自带 `isolation: isolate`，其他组件也应注意堆叠隔离
